//Exercise : There is a Matrix(n,n) with positive value.
//The code should return the best way to go from (1,1) to (n,n) and maximize the value of the path.
//The two directions available are DOWN and RIGHT
// For example: [[1,2][3,4]] has 8 for maximal path


const getOptimalValueOverMatrix = (Matrix) => {
    
}
